import React from 'react';

const CreditFactoryButton: React.FC = () => {
    const handleButtonClick = () => {
        // Logic to open credit factory bot
    };

    return (
        <button onClick={handleButtonClick}>
            Open Credit Factory Bot
        </button>
    );
};

export default CreditFactoryButton;
